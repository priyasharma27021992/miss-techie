type SizeUnit = "%" | "px" | "em" | "vh";

type ExperienceType = {
  title: string;
  description: string;
  time: string;
  company: string;
  location: string;
};

export type { SizeUnit, ExperienceType };
